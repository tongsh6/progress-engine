id: TS-0007
name: "first CLI state show slice working"
primary_dimension: implementation
source_gap: SG-0005
status: accepted
human_gate_required: true
desired_state: "第一条 Python CLI 切片 `progress state show` 可以读取 Project State，输出状态摘要，并有针对成功和失败路径的测试。"
from:
  maturity: seed
  summary: "第一条 CLI implementation slice 已定义，但尚未被代码实现和验证。"
to:
  maturity: drafted
  summary: "`progress state show` 作为只读 Python CLI 切片可运行、可测试，证明 v0.1 代码实现路径成立。"
required_evidence:
  - "Python package / CLI entrypoint 已创建。"
  - "`progress state show` 或等效模块入口可读取当前仓库 Project State 并输出摘要。"
  - "pytest 覆盖成功路径、缺文件路径和 malformed state 路径。"
  - "python3 scripts/check_repo.py 仍通过。"
acceptance_criteria:
  - id: AC-TS-0007-01
    criterion: "`progress state show` 能读取 `.progress/state/project_state.yaml` 并打印 project id、phase、dimension maturity、open gaps 和 next target。"
  - id: AC-TS-0007-02
    criterion: "命令不修改 `.progress/` 中任何文件。"
  - id: AC-TS-0007-03
    criterion: "缺少 Project State 文件时返回 exit code `2` 并输出清晰错误。"
  - id: AC-TS-0007-04
    criterion: "YAML 解析失败或必要字段缺失时返回 exit code `2` 并输出清晰错误。"
  - id: AC-TS-0007-05
    criterion: "pytest 覆盖成功路径、缺文件路径和 malformed state 路径。"
  - id: AC-TS-0007-06
    criterion: "`python3 scripts/check_repo.py` 仍通过。"
non_goals:
  - "不实现完整 CLI 命令集。"
  - "不实现 State Delta apply / rollback。"
  - "不生成 Fresh Context Capsule。"
  - "不接入模型 API、Web UI 或外部 agent。"
related_gaps:
  - SG-0005

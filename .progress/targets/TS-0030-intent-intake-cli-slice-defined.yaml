id: TS-0030
name: "intent intake CLI slice defined"
primary_dimension: implementation
source_gap: SG-0023
status: accepted
human_gate_required: true
desired_state: "下一条 bootstrap CLI slice 被定义为小范围、可测试的 `progress intake --from intent.md`。"
from:
  maturity: drafted
  summary: "`progress init --project PROJECT_ID` 已可创建空 `.progress/` 骨架；intent intake 尚未定义。"
to:
  maturity: drafted
  summary: "`progress intake --from FILE` 的用户路径、输入输出、状态影响、文件边界、验收标准和 out_of_scope 已明确。"
required_evidence:
  - "实现切片明确只覆盖一个最小 intent intake 写操作。"
  - "acceptance criteria 和 evidence_required 可验证。"
  - "out_of_scope 明确排除 assess、target suggestion、delta apply 和模型调用。"
acceptance_criteria:
  - id: AC-TS-0030-01
    criterion: "定义 `progress intake --from FILE` 的用户路径、输入、输出和状态影响。"
  - id: AC-TS-0030-02
    criterion: "定义该 slice 的测试和 Evidence 要求。"
  - id: AC-TS-0030-03
    criterion: "确认不会越界实现 assess、target suggestion、delta apply 或模型调用。"
non_goals:
  - "不在 target 定义阶段实现代码。"
  - "不实现自动状态评估。"
  - "不自动生成 Gap / Target / Intervention。"
related_gaps:
  - SG-0023

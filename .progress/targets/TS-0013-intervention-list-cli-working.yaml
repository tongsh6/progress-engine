id: TS-0013
name: "intervention list CLI slice working"
primary_dimension: implementation
source_gap: SG-0011
status: accepted
human_gate_required: true
desired_state: "第四条 Python CLI 切片 `progress intervention list` 可以读取 Intervention 对象目录，并输出未完成 Intervention 的只读摘要。"
from:
  maturity: drafted
  summary: "`progress intervention list` 已定义但尚未被接受为实现成果。"
to:
  maturity: drafted
  summary: "`progress intervention list` 作为只读 Python CLI 切片可运行、可测试，证明 Intervention 对象目录读取路径成立。"
required_evidence:
  - "CLI 支持 `progress intervention list`。"
  - "命令只读取 `.progress/interventions/*.yaml`。"
  - "pytest 覆盖成功路径、缺目录路径、malformed YAML 路径和缺必填字段路径。"
  - "python3 scripts/check_repo.py 仍通过。"
acceptance_criteria:
  - id: AC-TS-0013-01
    criterion: "`progress intervention list` 能读取 `.progress/interventions/*.yaml` 并输出未完成 Intervention 摘要。"
  - id: AC-TS-0013-02
    criterion: "命令默认不输出 `completed` Intervention。"
  - id: AC-TS-0013-03
    criterion: "interventions 目录缺失时返回 exit code `2` 并输出清晰错误。"
  - id: AC-TS-0013-04
    criterion: "Intervention YAML 解析失败或必要字段缺失时返回 exit code `2` 并输出清晰错误。"
  - id: AC-TS-0013-05
    criterion: "pytest 覆盖成功路径、缺目录路径、malformed YAML 路径和缺字段路径。"
  - id: AC-TS-0013-06
    criterion: "`python3 scripts/check_repo.py` 仍通过。"
non_goals:
  - "不实现 `progress plan`。"
  - "不实现 intervention create / update / close。"
  - "不实现 run lifecycle、Evidence、Delta 或模型 API。"
related_gaps:
  - SG-0011

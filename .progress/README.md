# .progress

这是 ProgressEngine 项目自身的状态账本，用于示范并约束“持续推进项目状态”的工作方式。

不要把 `.progress/` 当成普通待办清单。它记录的是：

```text
Project State → State Gap → Target State → Intervention → Run → Evidence → State Delta
```

当前阶段：repo bootstrap。

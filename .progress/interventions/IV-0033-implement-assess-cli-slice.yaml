id: IV-0033
name: "Implement assess CLI slice"
primary_dimension: implementation
target_state_id: TS-0033
status: completed
goal: "只实现 `progress assess` 这一条只读 assessment 命令，汇总 Project State maturity、open gaps 和 next targets。"
in_scope:
  - "扩展 argparse 命令 `progress assess`"
  - "读取 `.progress/state/project_state.yaml`"
  - "读取 Project State 声明的 open gaps"
  - "读取 Project State 声明的 next targets"
  - "输出确定性的 assessment 摘要"
  - "为成功路径、缺 Project State、Project State YAML 损坏、缺 gap 引用、缺 target 引用、gap YAML 损坏和 target YAML 损坏添加 pytest"
out_of_scope:
  - "不自动生成 Gap / Target / Intervention"
  - "不实现 target suggestion 或 scoring"
  - "不实现 state refresh"
  - "不在 `progress assess` 运行时生成 Evidence 对象或自动 apply State Delta"
  - "不调用模型 API、Web UI 或外部 agent"
evidence_required:
  - "python3 -m pytest tests/test_cli_assess.py 通过"
  - "python3 -m pytest 通过"
  - "python3 scripts/check_repo.py 通过"
  - "PYTHONPATH=src python3 -m progress_engine assess 输出当前项目 assessment 摘要"
automation_policy: human_gate_required

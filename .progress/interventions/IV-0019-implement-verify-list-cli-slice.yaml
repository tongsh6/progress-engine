id: IV-0019
name: "Implement read-only verify list CLI slice"
primary_dimension: implementation
target_state_id: TS-0019
status: completed
goal: "只实现 `progress verify list` 这一条只读 Python CLI 用户路径，验证 Verification review 队列读取路径。"
in_scope:
  - "扩展 argparse 命令 `progress verify list`"
  - "复用 Evidence loader 读取 `.progress/evidence/*.yaml`"
  - "读取 reviewer.result 和 claims[].acceptance_mapping[].status"
  - "输出 Verification review 摘要"
  - "为成功路径、空目录路径、缺目录路径、malformed YAML 路径、缺字段路径和缺 acceptance status 路径添加 pytest"
out_of_scope:
  - "不实现 progress verify --run"
  - "不生成 verification artifact"
  - "不生成 State Delta Proposal"
  - "不实现 delta apply、模型 API、Web UI 或外部 agent"
evidence_required:
  - "python3 -m pytest 通过"
  - "python3 scripts/check_repo.py 通过"
  - "PYTHONPATH=src python3 -m progress_engine verify list 输出 Verification review 摘要"
automation_policy: human_gate_required

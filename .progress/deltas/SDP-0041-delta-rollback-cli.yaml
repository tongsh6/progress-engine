state_delta_proposal:
  id: SDP-0041
  source_intervention: IV-0041
  run_id: RUN-20260520-IV-0041
  target_state_id: TS-0041
  verification_id: EV-0041
  status: applied
  proposed_at: "2026-05-20T01:45:00+08:00"
  proposed_by: verifier
  primary_dimension: implementation
  before:
    implementation:
      maturity: drafted
      summary: "`progress delta rollback` 已定义但尚未实现。"
      current_problem:
        - "State Delta apply 后尚不能通过受控 CLI 回退最新 applied proposal。"
  after:
    implementation:
      recommended_maturity: drafted
      summary: "`progress delta rollback` 已作为 human-gated allow-list State Delta rollback 写路径实现并测试通过。"
      advancement_reason:
        - "CLI 支持 `progress delta rollback SDP-ID --approved-by NAME`。"
        - "实现校验 applied proposal、rollback gate、apply metadata、state history、reversibility 和 allow-list restore。"
        - "成功路径恢复 Project State、追加 rollback state history，并标记 proposal rolled_back。"
        - "失败路径在写入前返回，避免部分状态更新。"
      caveat: "reject 和 state refresh 仍未实现；implementation maturity 保持 drafted。"
  evidence_refs:
    - "src/progress_engine/cli.py"
    - "src/progress_engine/deltas/delta_rollback.py"
    - "tests/test_cli_delta_rollback.py"
    - "tests/fixtures/minimal_progress_project/.progress/deltas/SDP-1003-rollback-ready-delta.yaml"
    - "tests/fixtures/minimal_progress_project/.progress/evidence/EV-1003-rollback-ready.yaml"
    - "README.md"
    - "src/progress_engine/README.md"
    - ".progress/evidence/EV-0041-delta-rollback-cli.yaml"
  acceptance_summary:
    pass: 6
    fail: 0
    not_tested: 0
  remaining_gaps:
    - id: SG-0032
      summary: "State Delta apply / rollback 已实现，但 state refresh 尚未定义下一条切片，reject 也仍保持 out_of_scope。"
      next_state_path: "Define the next state refresh-focused slice before implementing refresh or reject."
  recommended_next_interventions:
    - id: IV-0042
      name: "Define state refresh CLI slice"
      condition: "Run after SDP-0041 is accepted by human gate."
      primary_dimension: implementation
  requires_human_approval: true
  do_not_apply_automatically: true
  gate:
    required: true
    approved_by: human_user
    approved_at: "2026-05-20T01:45:00+08:00"
    decision: approved
  apply:
    allowed_before_human_approval: false
    applied_by: human_user
    applied_at: "2026-05-20T01:45:00+08:00"
    previous_state_version: PS-0039
    next_state_version: PS-0040
    project_state_file: ".progress/state/project_state.yaml"
    proposed_patch_summary: "Keep implementation maturity at drafted, add delta rollback implementation evidence, resolve SG-0031, open SG-0032 for state refresh slice definition, and set TS-0042 as the next target."
  rollback:
    reversible: true
    rollback_delta_id: null
    rollback_steps:
      - "Remove `progress delta rollback` command wiring and `src/progress_engine/deltas/delta_rollback.py`."
      - "Remove `tests/test_cli_delta_rollback.py` and the SDP-1003 / EV-1003 fixtures."
      - "Restore README command lists to omit `progress delta rollback`."
      - "Reopen SG-0031 and set Project State aim_of_next_state back to TS-0041."

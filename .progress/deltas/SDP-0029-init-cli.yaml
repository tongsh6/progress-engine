state_delta_proposal:
  id: SDP-0029
  source_intervention: IV-0029
  run_id: RUN-20260518-IV-0029
  target_state_id: TS-0029
  verification_id: EV-0029
  status: applied
  proposed_at: "2026-05-18T23:25:00+08:00"
  proposed_by: verifier
  primary_dimension: implementation
  before:
    implementation:
      maturity: drafted
      summary: "`progress init --project PROJECT_ID` 已定义但尚未实现。"
      current_problem:
        - "新项目无法通过 CLI 创建最小 `.progress/` 状态账本。"
        - "写操作路径尚未验证拒绝覆盖行为。"
  after:
    implementation:
      recommended_maturity: drafted
      summary: "`progress init --project PROJECT_ID`、`progress state show`、`progress state history`、`progress gaps list`、`progress target list`、`progress intervention list`、`progress run list`、`progress evidence list`、`progress verify list`、`progress delta list` 和 `progress event list` 已作为 Python CLI 切片可运行、可测试。"
      advancement_reason:
        - "src/progress_engine/cli.py 增加 argparse 命令 `init --project`。"
        - "src/progress_engine/init/init_project.py 实现最小骨架创建、project id 校验、拒绝覆盖和成功输出渲染。"
        - "tests/test_cli_init.py 覆盖成功路径、state show 读取路径、已有 .progress 路径和非法 project id 路径。"
        - "python3 scripts/check_repo.py 和全量 pytest 均通过。"
      caveat: "本切片只实现初始化骨架；intake、assess、state refresh 和 delta apply 尚未实现。"
  evidence_refs:
    - "src/progress_engine/cli.py"
    - "src/progress_engine/init/init_project.py"
    - "tests/test_cli_init.py"
    - ".progress/targets/TS-0029-init-cli-working.yaml"
    - ".progress/evidence/EV-0029-init-cli.yaml"
  acceptance_summary:
    pass: 6
    fail: 0
    not_tested: 0
  remaining_gaps:
    - id: SG-0023
      summary: "初始化后从 intent 文件引入初始意图的 CLI slice 尚未定义。"
      next_state_path: "Define a bounded intent intake CLI slice before implementing more bootstrap writes."
  recommended_next_interventions:
    - id: IV-0030
      name: "Define intent intake CLI slice"
      condition: "Run after init CLI is accepted."
      primary_dimension: implementation
  requires_human_approval: true
  do_not_apply_automatically: true
  gate:
    required: true
    approved_by: human_user
    approved_at: "2026-05-18T23:25:00+08:00"
    decision: approved
  apply:
    allowed_before_human_approval: false
    applied_by: human_user
    applied_at: "2026-05-18T23:25:00+08:00"
    previous_state_version: PS-0027
    next_state_version: PS-0028
    project_state_file: ".progress/state/project_state.yaml"
    proposed_patch_summary: "Keep implementation maturity at drafted, mark TS-0029 accepted, add TS-0029 / EV-0029 / SDP-0029 evidence refs, and set TS-0030 as the next target."
  rollback:
    reversible: true
    rollback_delta_id: null
    rollback_steps:
      - "If rejected, remove `progress init` command wiring and init module."
      - "Remove tests/test_cli_init.py and README update."

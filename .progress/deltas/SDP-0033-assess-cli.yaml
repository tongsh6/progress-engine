state_delta_proposal:
  id: SDP-0033
  source_intervention: IV-0033
  run_id: RUN-20260519-IV-0033
  target_state_id: TS-0033
  verification_id: EV-0033
  status: proposed
  proposed_at: "2026-05-19T00:15:00+08:00"
  proposed_by: verifier
  primary_dimension: implementation
  before:
    implementation:
      maturity: drafted
      summary: "`progress assess` 已定义但尚未实现。"
      current_problem:
        - "用户在 init / intake 后缺少一个汇总 Project State maturity、open gaps 和 next targets 的只读 assessment 命令。"
  after:
    implementation:
      recommended_maturity: drafted
      summary: "`progress assess` 已作为只读 assessment 命令实现并测试通过。"
      advancement_reason:
        - "src/progress_engine/cli.py 增加 argparse 命令 `assess`。"
        - "src/progress_engine/assessment/assess.py 复用现有只读 loader 组合 Project State、open gaps 和 next targets。"
        - "tests/test_cli_assess.py 覆盖成功路径、缺 Project State、Project State YAML 损坏、缺 gap 引用、缺 target 引用、gap YAML 损坏和 target YAML 损坏。"
        - "python3 scripts/check_repo.py 和全量 pytest 均通过。"
      caveat: "本切片不自动生成 Gap / Target / Intervention，不执行 state refresh 或 delta apply；implementation maturity 保持 drafted。"
  evidence_refs:
    - "src/progress_engine/cli.py"
    - "src/progress_engine/assessment/assess.py"
    - "tests/test_cli_assess.py"
    - ".progress/targets/TS-0033-assess-cli-working.yaml"
    - ".progress/evidence/EV-0033-assess-cli.yaml"
  acceptance_summary:
    pass: 6
    fail: 0
    not_tested: 0
  remaining_gaps:
    - id: SG-0026
      summary: "`progress assess` 已实现并通过验证；等待 human gate 后才能更新 Project State 和状态历史。"
      next_state_path: "Human gate review SDP-0033; accepted 后再应用 Project State 更新并决定下一条 target。"
  recommended_next_interventions: []
  requires_human_approval: true
  do_not_apply_automatically: true
  gate:
    required: true
    approved_by: null
    approved_at: null
    decision: pending
  apply:
    allowed_before_human_approval: false
    applied_by: null
    applied_at: null
    previous_state_version: PS-0030
    next_state_version: null
    project_state_file: ".progress/state/project_state.yaml"
    proposed_patch_summary: "Keep implementation maturity at drafted, mark TS-0033 accepted, resolve SG-0026, add TS-0033 / EV-0033 / SDP-0033 evidence refs, include `progress assess` in implementation summary, and set the next target after human review."
  rollback:
    reversible: true
    rollback_delta_id: null
    rollback_steps:
      - "If rejected, remove `progress assess` command wiring and assessment module."
      - "Remove tests/test_cli_assess.py and README update."
      - "Keep Project State unchanged because this proposal has not been applied."


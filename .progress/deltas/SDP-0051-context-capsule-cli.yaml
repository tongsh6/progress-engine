state_delta_proposal:
  id: SDP-0051
  source_intervention: IV-0051
  run_id: RUN-20260521-IV-0051
  target_state_id: TS-0051
  verification_id: EV-0051
  status: applied
  proposed_at: '2026-05-21T00:00:00+08:00'
  proposed_by: verifier
  primary_dimension: implementation
  before:
    implementation:
      maturity: drafted
      summary: Context Capsule CLI slice 已定义，但 `progress capsule --intervention IV-ID`
        尚未实现。
      current_problem:
      - AI 协作入口仍停留在手动整理上下文。
  after:
    implementation:
      recommended_maturity: drafted
      summary: '`progress capsule --intervention IV-ID` 已实现为 prompt-only Markdown
        Context Capsule 生成命令；不调用模型 API，不执行 Intervention，不修改 Project State 或 state
        history。'
      advancement_reason:
      - 新增 Context Capsule renderer 和 CLI parser。
      - 新增 focused pytest 覆盖成功路径、缺 intervention、缺 target、重复输出拒绝、非法 id、Project State
        缺失和 State History 缺失。
      - 实际生成 `.progress/context_capsules/IV-0051-context-capsule.md` 作为示例输出。
      - 全量 pytest 和仓库检查通过。
      caveat: prompt-only 执行仍缺少 Run lifecycle 写入入口。
  evidence_refs:
  - src/progress_engine/cli.py
  - src/progress_engine/capsule/context_capsule.py
  - tests/test_cli_capsule.py
  - .progress/context_capsules/IV-0051-context-capsule.md
  - docs/05-delivery/48-context-capsule-cli-run.md
  - .progress/evidence/EV-0051-context-capsule-cli.yaml
  - .progress/gaps/SG-0042-run-start-cli-slice-gap.yaml
  - .progress/targets/TS-0052-run-start-cli-slice-defined.yaml
  acceptance_summary:
    pass: 6
    fail: 0
    not_tested: 0
  remaining_gaps:
  - id: SG-0042
    summary: '`progress capsule --intervention IV-ID` 已能生成 prompt-only Context Capsule，但
      prompt-only 执行仍缺少可追踪 Run lifecycle 写入入口。'
    next_state_path: Define prompt-only run start CLI slice.
  recommended_next_interventions:
  - id: IV-0052
    name: Define prompt-only run start CLI slice
    condition: Run after SDP-0051 is accepted by human gate.
    primary_dimension: implementation
  requires_human_approval: true
  do_not_apply_automatically: true
  gate:
    required: true
    approved_by: human_user
    approved_at: '2026-05-21T00:00:00+08:00'
    decision: approved
  apply:
    allowed_before_human_approval: false
    proposed_patch_summary: Keep implementation maturity drafted, mark TS-0051 accepted,
      resolve SG-0041, add SG-0042 / TS-0052 / IV-0052 / EV-0051 / SDP-0051 refs,
      and set TS-0052 as the next target.
    applied_by: human_user
    applied_at: '2026-05-21T00:03:08+08:00'
    previous_state_version: PS-0049
    next_state_version: PS-0050
    project_state_file: .progress/state/project_state.yaml
  project_state_update:
    state_dimensions:
      implementation:
        maturity: drafted
        summary: '`progress init --project PROJECT_ID`、`progress intake --from FILE`、`progress
          assess`、`progress state show`、`progress state history`、`progress state refresh
          [--after-delta SDP-ID]`、`progress gaps list`、`progress target list`、`progress
          intervention list`、`progress capsule --intervention IV-ID`、`progress run
          list`、`progress evidence list`、`progress verify list`、`progress delta list`、`progress
          delta apply`、`progress delta rollback` 和 `progress delta reject` 已作为 Python
          CLI 切片可运行、可测试；State Delta lifecycle 已通过本地 pilot 验证。'
    open_state_gaps:
    - SG-0042
    aim_of_next_state:
    - TS-0052
  rollback:
    reversible: true
    rollback_delta_id: null
    rollback_steps:
    - If rejected, remove `progress capsule` implementation files and tests.
    - Restore Project State open_state_gaps to SG-0041 and aim_of_next_state to TS-0051.
    - Remove `.progress/context_capsules/IV-0051-context-capsule.md`.

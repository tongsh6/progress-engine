state_delta_proposal:
  id: SDP-0015
  source_intervention: IV-0015
  run_id: RUN-20260517-IV-0015
  target_state_id: TS-0015
  verification_id: EV-0015
  status: applied
  proposed_at: "2026-05-17T21:39:01+08:00"
  proposed_by: verifier
  primary_dimension: implementation
  before:
    implementation:
      maturity: drafted
      summary: "`progress run list` 已定义为第五条只读 CLI 切片，但尚未实现代码。"
      current_problem:
        - "CLI 尚不能列出未关闭 Run。"
  after:
    implementation:
      recommended_maturity: drafted
      summary: "`progress run list` 作为只读 Python CLI 切片已经可运行、可测试。"
      advancement_reason:
        - "src/progress_engine/cli.py 增加 argparse 命令 `run list`。"
        - "src/progress_engine/runs/run_list.py 实现只读 Run 加载、最小结构校验和摘要渲染。"
        - "pytest 覆盖成功路径、缺目录路径、malformed YAML 和缺字段路径。"
        - "Post-review pytest coverage explicitly verifies that both completed and abandoned runs are omitted."
        - "python3 scripts/check_repo.py 仍通过。"
      caveat: "本切片只证明 Run 目录读取路径；run lifecycle、Evidence 和 delta apply 尚未实现。"
  evidence_refs:
    - "src/progress_engine/cli.py"
    - "src/progress_engine/runs/run_list.py"
    - "tests/test_cli_run_list.py"
    - ".progress/evidence/EV-0015-run-list-cli.yaml"
  acceptance_summary:
    pass: 6
    fail: 0
    not_tested: 0
  remaining_gaps:
    - id: SG-0003
      summary: ".progress 对象最小一致性检查仍可继续收敛。"
      next_state_path: "Choose the next narrow CLI state path or quality slice after this accepted implementation slice."
  recommended_next_interventions:
    - id: IV-0016
      name: "Define next evidence object CLI slice"
      condition: "Run after SDP-0015 is accepted."
      primary_dimension: implementation
  requires_human_approval: true
  do_not_apply_automatically: true
  gate:
    required: true
    approved_by: human_user
    approved_at: "2026-05-17T22:00:39+08:00"
    decision: approved
  apply:
    allowed_before_human_approval: false
    applied_by: human_user
    applied_at: "2026-05-17T22:00:39+08:00"
    previous_state_version: PS-0013
    next_state_version: PS-0014
    project_state_file: ".progress/state/project_state.yaml"
    proposed_patch_summary: "If approved, keep implementation maturity at drafted and add TS-0015 / EV-0015 / SDP-0015 evidence refs."
  rollback:
    reversible: true
    rollback_delta_id: null
    rollback_steps:
      - "If rejected, keep implementation maturity at drafted."
      - "Revise or revert the `progress run list` implementation while preserving accepted previous slices."

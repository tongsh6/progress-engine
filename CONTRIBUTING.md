# Contributing / Working Agreement

ProgressEngine 的协作原则是 **State-first, Evidence-backed**。

## 工作规则

1. 任何推进动作必须说明它推动哪个项目状态维度。
2. 任何代码或文档变更必须能映射到一个 Intervention、State Gap、Change Event 或 State Delta。
3. PR 不能只说“完成了什么任务”，必须说明项目状态发生了什么变化。
4. 对产品、架构、质量协议的修改必须记录影响范围。
5. 重大变化应先产生 Change Event，再由 Target State / Intervention 承接。
6. State Delta 只能通过 proposal / review / apply 流程写入项目状态。

## PR 检查重点

- 是否符合“持续推进项目状态”的总纲；
- 是否引入任务中心化叙事；
- 是否有 evidence；
- 是否存在 silent deferral；
- 是否需要补 Change Event、State Gap 或 Target State；
- 是否影响 Fresh Context / Evidence Verifier / State Delta 协议。

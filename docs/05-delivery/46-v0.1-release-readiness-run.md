# v0.1 Release Readiness Run

本文记录 `IV-0049: Run v0.1 release readiness checks` 的执行结果。该 run 按 `docs/05-delivery/45-v0.1-release-readiness-slice.md` 定义的边界验证 v0.1 本地发布前检查、editable install 和 CLI 试用路径。

## 1. Run 结论

v0.1 release readiness checks 通过：

- 全量 pytest 通过。
- 仓库一致性检查通过。
- editable install smoke test 通过。
- 安装后的 `progress` console script 能运行 `progress assess`。
- 空目录 bootstrap 试用路径通过，覆盖 `init`、`intake`、`state show`、`assess`、`gaps list`、`target list` 和 `intervention list`。
- 仓库内主要只读 CLI 观察入口可运行。

本轮没有修改 CLI runtime 行为，没有引入 Web UI、SaaS、多用户、模型 API 或外部 agent。

## 2. 命令结果

### 2.1 全量测试

```bash
python3 -m pytest
```

结果：

```text
122 passed in 0.69s
```

测试集中包含 bootstrap pilot 和 State Delta lifecycle pilot：

- `tests/test_pilot_validation.py`
- `tests/test_state_delta_lifecycle_pilot.py`

### 2.2 仓库检查

```bash
python3 scripts/check_repo.py
```

结果：

```text
[OK] required paths exist
[OK] YAML parse passed
[OK] JSONL parse passed
[OK] local Markdown links passed
[OK] .progress object checks passed
[OK] Project State reference checks passed
[OK] CLI status documentation checks passed
```

### 2.3 Editable Install Smoke Test

首次在受限沙箱中执行 editable install 时，pip 构建依赖下载被网络策略阻止：

```text
ERROR: Could not find a version that satisfies the requirement setuptools>=68
```

该失败属于环境网络限制，不是项目包元数据或 CLI 入口失败。按执行策略放开网络后，smoke test 通过：

```bash
python3 -m venv /private/tmp/progress-engine-release.*/venv
/private/tmp/progress-engine-release.*/venv/bin/python -m pip install -e .
/private/tmp/progress-engine-release.*/venv/bin/progress assess
```

关键结果：

```text
Successfully installed PyYAML-6.0.3 progress-engine-0.1.0

Assessment:
Project: progress-engine
Phase: repo_bootstrap

Open gaps:
- SG-0039 [delivery] v0.1 release readiness checks 被执行，安装和试用路径有可审查 evidence。
```

### 2.4 Bootstrap Trial

在安装后的 venv 中创建空目录试用：

```bash
progress init --project sample-tool
progress intake --from intent.md
progress state show
progress assess
progress gaps list
progress target list
progress intervention list
```

关键结果：

```text
Initialized ProgressEngine project: sample-tool
Captured intent: .progress/artifacts/intent.md
Updated Project State intent maturity: seed

Project: sample-tool
Phase: initialized
Dimensions:
- intent: seed
- product: unknown
- design: unknown
- architecture: unknown
- implementation: unknown
- quality: unknown
- delivery: unknown
- knowledge: unknown

Open gaps:
- none
Next target:
- none
```

### 2.5 Repository CLI Observation

仓库内观察命令通过：

```bash
PYTHONPATH=src python3 -m progress_engine assess
PYTHONPATH=src python3 -m progress_engine state show
PYTHONPATH=src python3 -m progress_engine state history
PYTHONPATH=src python3 -m progress_engine run list
PYTHONPATH=src python3 -m progress_engine evidence list
PYTHONPATH=src python3 -m progress_engine verify list
PYTHONPATH=src python3 -m progress_engine delta list
PYTHONPATH=src python3 -m progress_engine event list
```

关键观察：

```text
Project: progress-engine
Phase: repo_bootstrap

Maturity:
- delivery: weak

Open gaps:
- SG-0039 [delivery] v0.1 release readiness checks 被执行，安装和试用路径有可审查 evidence。

Next targets:
- TS-0049 [delivery] v0.1 release readiness accepted
```

## 3. Acceptance Mapping

| Criterion | Result | Evidence |
|---|---|---|
| 全量 pytest 和仓库检查通过。 | pass | `python3 -m pytest`、`python3 scripts/check_repo.py` |
| editable install smoke test 能运行 `progress assess`。 | pass | `/private/tmp/.../venv/bin/progress assess` |
| 本地 CLI 试用路径覆盖 init、intake、state show、assess 和主要 list 命令。 | pass | Bootstrap trial output |
| Evidence 记录命令结果和失败处理边界。 | pass | 本文与 `EV-0049` |
| 本轮不引入 Web UI、SaaS、多用户、模型 API 或外部 agent。 | pass | git diff 与 scope check |

## 4. Scope Check

本轮只新增 delivery run 文档和 `.progress/` 账本对象。没有修改：

- `src/progress_engine/`
- `tests/`
- `pyproject.toml`
- CLI 命令语义
- Project State write gate 规则

未引入：

- Web UI
- SaaS
- 多用户协作
- 模型 API
- 外部 agent
- 发布自动化

## 5. Remaining Gap

v0.1 release readiness 已经具备本地 evidence，可以把 delivery maturity 从 `weak` 提升到 `reviewed`。

下一条更自然的项目缺口是 AI 协作入口仍停留在 prompt-only / manual-run：项目已有 Fresh Context 和 Context Capsule 设计，但还没有可运行的 `progress capsule` CLI 切片。因此后续应定义并实现 Context Capsule CLI，而不是直接接入模型 API。
